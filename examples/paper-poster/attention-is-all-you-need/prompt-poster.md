# Poster Prompt — Attention Is All You Need (conference-wide)

Generated by paper-poster Steps 3-4. Ready to feed into any image generation backend.

---

Create one complete academic conference poster image at 48x36 inches, 300dpi (14400x10800px).

Style preset: conference-wide
Orientation: landscape (4:3)

---

**TITLE BANNER (12% — top full width):**
"Attention Is All You Need"
Bold, 96pt sans-serif, dark navy (#1e3a5f)
Subtitle: "Transformer — Sequence Transduction Without Recurrence"
Authors: Vaswani et al., Google · NeurIPS 2017

**LEFT COLUMN (30% width):**

Panel 1 — PROBLEM & MOTIVATION (18% of poster height):
- RNNs process tokens sequentially -> slow training
- Long-range dependencies require O(n) steps
- Previous architectures (LSTM, ConvS2S) can't fully parallelize
- "We propose a new architecture based solely on attention"

Panel 2 — METHOD PIPELINE (15%):
[Small simplified flow at bottom of left column]
Input -> Embed+PosEnc -> [Multi-Head Self-Attention] -> Add&Norm -> FFN -> Add&Norm -> (x6) -> Output

**CENTER-RIGHT (70% width) — MAIN METHOD FIGURE (38%):**
[THE LARGEST VISUAL ELEMENT — must dominate]
Full Transformer architecture diagram:

Encoder stack (left):
- Input Embedding + Positional Encoding
- Multi-Head Self-Attention (highlighted box)
- Add & Layer Normalization
- Feed-Forward Network
- Add & Layer Normalization
- (x6 repeated)

Decoder stack (right):
- Output Embedding + Positional Encoding
- Masked Multi-Head Self-Attention
- Multi-Head Cross-Attention (attends to encoder output)
- Add & Layer Normalization
- Feed-Forward Network
- Add & Layer Normalization
- (x6 repeated)

Output: Linear -> Softmax -> Output Probabilities

Style: Clean vector diagram, professional academic figure quality
Color: Dark blue lines, white/light gray boxes, blue (#2563eb) highlight on attention modules
Labels: 24pt sans-serif, each block labeled with 2-4 words

**LEFT COLUMN BOTTOM (return to left) — KEY RESULTS (20%):**
Three large data cards side by side:
┌────────────────────┬────────────────────┬────────────────────┐
│   BLEU 28.4        │  3.5 days          │  O(1) paths        │
│  EN-DE Translation │  8× P100 GPU       │  Any position      │
│  New SOTA          │  vs weeks prior     │  vs RNN O(n)       │
└────────────────────┴────────────────────┴────────────────────┘
Numbers: 56pt bold, blue (#2563eb)
Labels: 20pt

**CENTER-RIGHT BOTTOM — SELF-ATTENTION DETAIL (10%):**
Single-head attention formula:
Attention(Q,K,V) = softmax(QK^T / sqrt(d_k)) V
Multi-head: Concat(head_1,...,head_h) W^O
Mathematical notation, clean, no full derivation

**BOTTOM BAR (5% — full width):**

CONCLUSION: "Transformer achieves new translation SOTA with pure attention, trains in days, and generalizes to diverse sequence tasks."
REFERENCES (4):
[1] Vaswani et al., NeurIPS 2017 — original paper
[2] Devlin et al., BERT — pretraining applied
[3] Brown et al., GPT-3 — scaled up
[4] Dosovitskiy et al., ViT — vision adaptation

---

**GLOBAL VISUAL CONSTRAINTS:**
- Background: white (#ffffff), no paper texture or gradients
- Color palette: dark navy (#1e3a5f), blue accent (#2563eb), light gray (#f3f4f6), dark text (#1a1a1a)
- Typography: clean sans-serif throughout (Inter or Helvetica Neue style)
- All body text >= 24pt, all labels >= 20pt
- Method diagram is the central focus — must occupy 38% of poster height
- Do NOT generate: logos, watermarks, page numbers, decorative borders, fake data
- Do NOT generate: 3D effects, neon colors, cartoon elements, stock photos
- Maintain clear visual hierarchy: title > method diagram > data > details
